# infrastructure-exception-global

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build infrastructure-exception-global` to build the library.

## Running unit tests

Run `nx test infrastructure-exception-global` to execute the unit tests via [Jest](https://jestjs.io).
