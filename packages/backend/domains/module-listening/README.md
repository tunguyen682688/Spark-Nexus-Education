# module-listening

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build module-listening` to build the library.

## Running unit tests

Run `nx test module-listening` to execute the unit tests via [Jest](https://jestjs.io).
