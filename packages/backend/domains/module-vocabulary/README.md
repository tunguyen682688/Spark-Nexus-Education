# module-vocabulary

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build module-vocabulary` to build the library.

## Running unit tests

Run `nx test module-vocabulary` to execute the unit tests via [Jest](https://jestjs.io).
