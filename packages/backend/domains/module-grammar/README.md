# module-grammar

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build module-grammar` to build the library.

## Running unit tests

Run `nx test module-grammar` to execute the unit tests via [Jest](https://jestjs.io).
