# module-certification

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build module-certification` to build the library.

## Running unit tests

Run `nx test module-certification` to execute the unit tests via [Jest](https://jestjs.io).
